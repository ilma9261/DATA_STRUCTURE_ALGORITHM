// leetcode 541

class Solution {
public:
    string reverseStr(string s, int k) {
        int n=s.size();
       int i=0;
       int r=min(n,k);
       while(i<=n){
        reverse(s.begin()+i,s.begin()+r);
        i+=2*k;
        r=min(n,i+k);
       }
       return s;
    }
};
