//gfg

class Solution {
  public:
    int largest(vector<int> &arr) {
        // code here
        int mx=arr[0];
        int n=arr.size();
        for(int i=0; i<n; i++){
            mx=max(mx,arr[i]);
        }
        return mx;
    }
